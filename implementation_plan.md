# チケット価格予測 AI モデル：進化の軌跡・現状分析と今後の発展検証プラン

本計画書は、これまでの開発・研究によって進行した **Model 1〜12 のモデル進化の歴史・課題と対策の変遷** を、「モデル自体の構成・手法の変化」と「入力データの変化」を明確に分けて体系的に整理し、**最新の評価手法（相対誤差・百分率誤差）に基づいた現状分析と歴代世代比較** を行い、さらに今後推進すべき **「期間・時間軸（リードタイム）」による精度検証と改善プラン**、および **「フォルダ・モデル群の構成整理計画」** を策定した総合ドキュメントです。

---

## 1. 歴代モデルの進化軌跡と課題克服の歴史（構成 vs データ入力）

| 世代 / モデル | ① モデル自体の構成・推論手法の変化 | ② 入力データの変化・特徴量エンジニアリング | 当時発生していた問題点・限界 | 解決・発展させた成果 |
|---|---|---|---|---|
| **第1世代**<br>初期表データモデル<br>(Model 4〜7 等) | ・**LightGBM 単体回帰**<br>・目的変数を対数変換 `log(price)` して学習・推論 | ・**定型的な表データのみ入力**<br>  （公演日、会場名、チケット枚数、曜日、開演時間など）<br>・※ 説明文（`raw_description`）は未入力 | ・**説明文内の座席詳細（番手、同行等）を完全に無視**<br>・同じ公演であれば、アリーナ最前列も天井席もすべて「同じ平均価格」として出力してしまい、価格の分散を説明できなかった | ・説明文テキスト（自然言語）の情報をAIモデルに入力・理解させる NLP アプローチへの移行を決定 |
| **第2世代**<br>テキストNLP・LLMハイブリッド<br>(Model 8〜9 等) | ・**BERT 埋め込みモデル** によるテキストベクトル抽出<br>・**LLM (Qwen)** を用いた説明文からの価格推論<br>・表データ ＋ BERT ＋ LLM をアンサンブルする **LightGBM ハイブリッド構成** | ・**【テキストデータの入力追加】**<br>  出品者の説明文 (`raw_description`) 全文を入力対象に追加<br>・**【極限クレンジングデータの採用】**<br>  未売却・交渉中・手渡し・空テキスト・異常な外れ値データを除外した `sold` チケットのみのクリーンデータに変更<br>・**【需給特徴量の追加】**<br>  公演ごとの出品総数 (`event_listing_total`) や売却率 (`event_sold_ratio`) 等のマクロ指標を入力に追加 | ・BERTベクトルは文章の抽象的な文脈は捉えても、「1番手＝＋3万円」「ランダム＝−1万円」といった**ジャニーズ市場特有の金額ルールを正確な数値として学習しきれなかった** | ・データ品質の純度を極限まで高めることで、ノイズによる誤予測を排除<br>・市場全体の需給バランスをモデルが考慮可能になった |
| **第3世代**<br>ドメイン知識構造化＋ハイブリッド<br>(Model 10〜12: 現状) | ・LLM推論を実価格に補正する **Isotonic Regression**<br>・補正済みLLM ＋ BERT ＋ 構造化表データの **統合 LightGBM (Optuna最適化)**<br>・**「相対誤差 (MAPE・％)」による評価手法の導入** | ・**【ドメイン知識パーサー (`description_parser.py`) の統合】**<br>  説明文からルールベースで以下を抽出し、**明確な構造化特徴量として新規追加**：<br>  - `bante` (番手), `total_meigi` (総名義数)<br>  - `random_type` (少人数ランダム等)<br>  - `surikae_nashi` (すり替えなし保証)<br>  - `doukou_type` (同行/QR毎ログイン譲渡)<br>  - `tousen_type` (初期当選/復活当選等)<br>  - ゲート情報, 名義信頼性指標<br>・**【未来情報リークの除外】** (`listing_duration_days` 除外) | ・金額（円）差分による評価では、高額帯がすべて不適合に見えてしまい実力を測定できていなかった | ・相対誤差評価への刷新で、**メイン層(1.5万〜7.5万円)において±20%以内適合率 60〜86%・MAPE 11〜18%** という高い実用精度を立証<br>・安価帯・プレミアム帯のバイアスの正体を解明 |

---

## 2. 歴代世代（第1世代〜第3世代）の定量精度比較（金額誤差 vs 割合誤差）

同一の学習データ（全6,049件の純粋クリーンデータ）上で特徴量のみを変化させ、歴代世代ごとの性能進化を検証しました。

### 2.1 総合パフォーマンス比較表
| 世代 | 金額誤差 MAE (円) | 割合誤差 MAPE (%) | 中央値割合誤差 MdAPE (%) | 誤差 ±20%以内 適合率 (%) | 誤差 1,000円以内 適合率 (%) | 決定係数 R² |
|---|---|---|---|---|---|---|
| **第1世代 (表データのみ)** | 6,672 円 | 33.40 % | 18.34 % | 53.1 % | 13.1 % | 0.6756 |
| **第2世代 (表+BERT+LLM)** | 5,719 円 | 25.30 % | 16.04 % | 58.4 % | 16.7 % | 0.7456 |
| **第3世代 (現行Model 12)** | **5,709 円** | **25.50 %** | **15.93 %** | **58.7 %** | **15.9 %** | **0.7478** |

### 2.2 5,000円価格帯別：データ件数および歴代世代 MAPE (%) 割合誤差 比較表
各価格帯のサンプル数（件数）と、世代ごとの割合誤差（MAPE）の推移一覧表です。**第3世代は1.5万〜7.5万円のメイン層において全世代で最も誤差率が低い結果** となっています。

| 価格帯 (円) | データ件数 (件) | 全体割合 | 第1世代 MAPE (%) | 第2世代 MAPE (%) | 第3世代 MAPE (%) 【現行】 | 評価・改善実態 |
|---|---|---|---|---|---|---|
| **0 〜 5,000** | 220 件 | 3.6 % | 148.7 % | 58.4 % | **58.6 %** | テキスト・需給導入で大幅改善も過大バイアス残存 |
| **5,000 〜 10,000** | 530 件 | 8.8 % | 67.6 % | 46.3 % | **47.6 %** | 大幅改善 (-20.0%) |
| **10,000 〜 15,000** | 888 件 | 14.7 % | 49.3 % | 40.2 % | **41.1 %** | 大幅改善 (-8.2%) |
| **15,000 〜 20,000** | 523 件 | 8.6 % | 28.2 % | 24.3 % | **24.1 %** | 🟢 良好領域へ到達 |
| **20,000 〜 25,000** | 701 件 | 11.6 % | 22.6 % | 20.4 % | **20.7 %** | 🟢 極めて良好 |
| **25,000 〜 30,000** | 895 件 | 14.8 % | 21.6 % | 18.3 % | **18.3 %** | 🟢 極めて良好 (MAPE 18%) |
| **30,000 〜 35,000** | 275 件 | 4.5 % | 17.0 % | 16.0 % | **16.0 %** | 🟢 極めて良好 |
| **35,000 〜 40,000** | 609 件 | 10.1 % | 15.7 % | 16.7 % | **16.7 %** | 🟢 極めて良好 |
| **40,000 〜 45,000** | 276 件 | 4.6 % | 15.7 % | 15.0 % | **15.0 %** | 🟢 極めて良好 (MAPE 15%) |
| **45,000 〜 50,000** | 131 件 | 2.2 % | 15.0 % | 11.0 % | **10.9 %** | 🌟 **最高精度到達 (MAPE 10.9%)** |
| **50,000 〜 55,000** | 284 件 | 4.7 % | 16.5 % | 13.7 % | **12.9 %** | 🌟 **ドメイン構造化でさらに1.8%改善** |
| **55,000 〜 60,000** | 220 件 | 3.6 % | 16.9 % | 14.0 % | **13.5 %** | 🟢 ドメイン構造化で0.5%改善 |
| **60,000 〜 65,000** | 262 件 | 4.3 % | 20.0 % | 15.5 % | **15.4 %** | 🟢 良好維持 |
| **65,000 〜 70,000** | 44 件 | 0.7 % | 17.6 % | 15.7 % | **15.6 %** | 🟢 良好維持 |
| **70,000 〜 75,000** | 104 件 | 1.7 % | 16.9 % | 16.7 % | **16.2 %** | 🟢 ドメイン構造化で0.5%改善 |
| **75,000 〜 80,000** | 66 件 | 1.1 % | 23.3 % | 22.9 % | **22.8 %** | 🟡 極端高額帯の縮退バイアス発生開始 |
| **80,000円 以上** | 22 件 | 0.4 % | 51.6〜88.5 % | 61.1〜85.1 % | **58.5〜85.3 %** | 🔴 極めて稀少な超プレミアムチケット（全体わずか0.4%）の過小予測 |
| **合計 / 全体** | **6,049 件** | **100.0 %** | **33.40 %** | **25.30 %** | **25.50 %** | **※ 中央値誤差 MdAPE は歴代最高(15.93%)** |

### 2.3 5,000円価格帯別：歴代世代 MAE (円) 金額誤差 比較表
「金額（円）の絶対誤差」での推移表です。チケット額面が高くなるほど比例して金額誤差も増大しますが、**第2世代・第3世代において第1世代（表データのみ）の巨大な誤差を劇的に抑制している** ことが確認できます。

| 価格帯 (円) | データ件数 (件) | 第1世代 MAE (円) | 第2世代 MAE (円) | 第3世代 MAE (円) 【現行】 | 第1世代からの削減額 (改善効果) |
|---|---|---|---|---|---|
| **0 〜 5,000** | 220 件 | 5,465 円 | 2,108 円 | **2,119 円** | **-3,346 円の大幅削減** |
| **5,000 〜 10,000** | 530 件 | 4,569 円 | 3,194 円 | **3,294 円** | **-1,275 円の削減** |
| **10,000 〜 15,000** | 888 件 | 5,952 円 | 4,863 円 | **4,970 円** | **-982 円の削減** |
| **15,000 〜 20,000** | 523 件 | 4,894 円 | 4,201 円 | **4,169 円** | **-725 円の削減** |
| **20,000 〜 25,000** | 701 件 | 4,812 円 | 4,341 円 | **4,399 円** | **-413 円の削減** |
| **25,000 〜 30,000** | 895 件 | 5,903 円 | 5,025 円 | **5,027 円** | **-876 円の削減** |
| **30,000 〜 35,000** | 275 件 | 5,518 円 | 5,205 円 | **5,223 円** | **-295 円の削減** |
| **35,000 〜 40,000** | 609 件 | 5,903 円 | 6,279 円 | **6,308 円** | 同等水準維持 |
| **40,000 〜 45,000** | 276 件 | 6,852 円 | 6,557 円 | **6,531 円** | **-321 円の削減** |
| **45,000 〜 50,000** | 131 件 | 7,140 円 | 5,284 円 | **5,236 円** | 🌟 **-1,904 円の大幅削減** |
| **50,000 〜 55,000** | 284 件 | 8,369 円 | 6,940 円 | **6,570 円** | 🌟 **-1,799 円の大幅削減 (第3世代最高)** |
| **55,000 〜 60,000** | 220 件 | 9,490 円 | 7,837 円 | **7,560 円** | 🌟 **-1,930 円の大幅削減 (第3世代最高)** |
| **60,000 〜 65,000** | 262 件 | 12,427 円 | 9,640 円 | **9,545 円** | 🌟 **-2,882 円の超劇的削減** |
| **65,000 〜 70,000** | 44 件 | 11,971 円 | 10,694 円 | **10,637 円** | **-1,334 円の削減** |
| **70,000 〜 75,000** | 104 件 | 11,909 円 | 11,795 円 | **11,432 円** | **-477 円の削減 (第3世代でさらに改善)** |
| **75,000 〜 80,000** | 66 件 | 18,136 円 | 17,808 円 | **17,726 円** | **-410 円の削減** |
| **80,000円 以上** | 22 件 | 51,834〜132,728円 | 51,836〜127,712円 | **49,571〜127,889円** | 超極稀プレミアム席の縮退傾向 |
| **全体平均 MAE** | **6,049 件** | **6,672 円** | **5,719 円** | **5,709 円** | **全体で -963 円の平均削減に成功** |

---

## 3. 現状分析：Model 12 の真の実力とバイアス構造

1. **実用的なボリュームゾーン（15,000円〜75,000円）で極めて高い有効性**
   - 市場の取引主流である 1.5万円〜7.5万円において、**予測の60〜86%が実際の価格の±20%以内に収まっており、平均誤差率（MAPE）も11〜18%** に達している。
   - 特に 45,000〜50,000円帯では **MAPE 10.9%・±20%以内的中率 86.3%** を誇る。
2. **モデルに存在する2つの構造的バイアス**
   - **低価格帯バイアス（〜15,000円）**: `+30% 〜 +58%` の過大予測傾向（安すぎるチケットを平均層に引き上げる傾向）。
   - **極端な高額プレミアム帯バイアス（80,000円〜）**: `-58% 〜 -87%` の過小予測傾向（極めて稀少な超高額席を全体平均側に引き戻す「回帰への縮退」）。

---

## 4. 今後の取り組み方：期間・時間軸（Timeline & Lead-time）検証プラン

モデル構成の結論を受け、次の発展章として **「期間・時間軸（リードタイム）」** を軸とした検証を推進します。

### Phase 1: 公演までの日数（リードタイム）別パフォーマンス検証
チケット取引市場は「イベントまでの残り日数」によって需給と心理が劇的に変化します。

- **検証指標**: `days_to_event = (event_date - created_at_date)` を正確に算出
- **期間帯の分割構成**:
  1. `30日以上前` (当落直後の本命・超早期期)
  2. `14〜30日前` (相場の収束・安定期)
  3. `7〜14日前` (直前調整期)
  4. `1〜7日前` (投げ売り・高額売買が交束するボラティリティ激変期)
- **分析・成果物**:
  - 各期間帯ごとの **MAPE(%)・±20%以内適合率(%)・平均バイアス(%)** を集計・視覚化する専用スクリプト `analyze_leadtime_dynamics.py` の実装。
  - 「開催2〜3週間前が最も相場が収束して精度が高く、直前は処分売り等で誤差が広がる」といった市場心理と精度の関係を論文で証明。

### Phase 2: データ蓄積期間によるプレミアム帯・希少データの改善検証
- **処理内容**:
  - サンプル数の継続的な蓄積によって、現状課題となっている「8万円以上の高額帯」や「少数データグループ」がどう改善するかを測定する実装・評価。

---

## 5. フォルダ構成の整理・アーカイブ集約計画 (Workspace Refactoring Plan)

開発プロセスで生まれた多数のモデルフォルダを整理し、**「論文や研究の節目となった代表的なモデル」** を1つの専用フォルダに集約し、それ以外の過渡期・中間実験モデルを `archive` フォルダへ集約します。

### 5.1 新フォルダ構成（予定ツリー）
```text
C:\Users\hero\Documents\tiketen_scraper\
├── representative_models\        ← 【新設】代表的なモデルを1階層に集約
│   ├── hybrid_AI_model7          ← [第1世代代表] 初期・表データ中心決定木モデル
│   ├── hybrid_AI_model8          ← [第2世代代表] BERT+LLM ハイブリッド基本確立モデル
│   ├── hybrid_AI_model9          ← [第2世代代表] 需給特徴量＆クリーンデータ拡張モデル
│   └── hybrid_AI_model12         ← [第3世代代表] 現行最新モデル（ドメイン知識構造化パーサー統合）
│
├── archive\                      ← 【集約先】実験途中・過渡期の旧モデルを保存
│   ├── hybrid_AI_model4          ← 初期試行モデル (移動)
│   ├── hybrid_AI_model9_1        ← Model 9 派生実験版 (移動)
│   ├── hybrid_AI_model9_2        ← Model 9 派生実験版 (移動)
│   ├── hybrid_AI_model10         ← Model 12 開発前段階プロトタイプ (移動)
│   └── ai_dev / model_1〜6 等     ← (既存のアーカイブ済みフォルダ)
│
├── data\                         ← メインスクレイピングデータ
├── tiketen_date_data\            ← 日付別スナップショットデータ
├── 手動_data\                    ← マスター定義CSV (アーティスト, 会場等)
├── scraper.py                    ← クローラー本体
├── update_masters.py             ← マスター更新スクリプト
└── plan.md / README.md           ← 総合計画書・ドキュメント
```

### 5.2 代表モデルの選定根拠
- **`hybrid_AI_model7`**: 表データのみで回帰した時代の完成形（第1世代のベースライン比較対象）。
- **`hybrid_AI_model8`**: 説明文を BERT ベクトル ＋ LLM (Qwen) の推論結果として初めて LightGBM とアンサンブルした節目モデル。
- **`hybrid_AI_model9`**: 表データ・テキスト・市場全体の需給特徴量（売却率や出品数等）を完全に融合させた第2世代の主力モデル。
- **`hybrid_AI_model12`**: ジャニーズ特有の金額ルールを抽出するドメイン知識パーサー (`description_parser.py`) を組み込み、相対誤差分析と歴代比較を行う現行モデル。

### 5.3 移行に伴う技術的対策（パス参照の自動維持）
- フォルダを `representative_models/` 配下に移動した際、各モデル内の `config.py` が `PROJECT_ROOT` を親フォルダ階層（`..`）に依存して取得している場合、参照先のずれが発生しないようルート探索パスを自動調整します。

---

## 6. Model 15：意味特徴復活・価格帯バイアス補正版

Model 14の評価低下分析を踏まえ、`hybrid_AI_model15` を次の方針で構築した。

### 6.1 データ方針

- 目的は「売れたチケットの成立価格予測」に限定し、最新snapshotのsoldデータへModel 13と同じ意味のあるクレンジングを適用する。
- 未売却データは価格妥当性以外の要因を含むため、価格回帰の教師データには混ぜない。
- 説明文が空または5文字未満の行を除外する。
- `専用`、`代理`、`取り置き`、`ダミー`、`相場理解`、`即決額`、`手渡し`、別途決済など、成立価格として解釈できない取引ノイズを除外する。
- 価格が2,000円未満または150,000円超の行を除外する。
- 各公演内の価格分布から下位5%・上位2%を外れ値として除外する。
- 売却時刻・掲載終了後に確定する値・目的価格由来の集計は入力から除外する。
- 説明文、出品時点で分かる日時、会場・公演、枚数、名義・本人確認、座席条件、出品時点までのas-of相場は使用する。

### 6.2 Model 14からの改善

- Model 14で欠落していたLLM JSON意味特徴を復活。座席階層、列位置、当選経路、名義状態、本人確認、配布方法、視界条件、FC初期当選、ランダム条件を明示的な特徴量にする。
- Model 13 JSONに含まれていた `price_estimate` は、説明文から価格ラベルを推測した派生値で比較を曖昧にするためModel 15では禁止し、読み込み時にも強制削除する。
- BERT埋め込みはModel15内でticket IDと入力文hashが一致する行だけ再利用し、新規・更新行は再抽出する。入力文hashを持たない旧Model14キャッシュは、更新後データに対する同一性を証明できないため流用しない。
- Qwen価格OOFと重複グループfoldは母集団変更後に作り直し、Model 14の全sold用OOFを流用しない。
- 正規表現ドメインパーサー、BERT文脈、Qwen価格OOF、LLM JSON意味特徴、表データ、as-of市場特徴をLightGBMで統合する。
- Model 14の高価格帯過小予測に対し、通常log-L1に加えて価格重み付きlog目的とraw-price Huberを同一OOFで比較する。raw Huberや固定blendが悪化した場合は自動採用しない。
- 意味特徴あり／なしを同一fold・同一目的関数で評価するアブレーションを必ず保存し、JSON特徴の寄与を数値で確認できるようにする。
- Model 13で有効だったLightGBM Optuna探索を復活し、fold-local前処理済み行列を全trialで再利用する。最終版では6.6の共同120 trial探索へ拡張した。

### 6.3 学習効率とハードウェア

- BERTのraw埋め込みは入力文hash一致行だけを安全に再利用し、最新snapshotで追加・更新された行を専用GPUで抽出する。価格ラベルを学習するQwenはクレンジング後データで再学習する。
- 拡張意味JSON抽出はNVIDIA CUDA device 0を明示使用し、Intel内蔵GPUには割り当てない。
- Qwen 7Bを4bit NF4で専用VRAMに配置し、標準4件batchで生成する。VRAM不足時はbatchを自動的に半減する。
- 意味JSONは20件ごとに保存し、再実行時はQwen15抽出済み行を保持して未完了分から再開する。
- LightGBMは全CPUスレッドを使用し、early stoppingで無効な追加反復だけを停止する。データ件数、fold数、特徴量は削減しない。
- Qwenは4bit NF4、5 epochs、5-foldを維持し、実効batch 32のままVRAM不足時だけ物理batchとgradient accumulationを等価交換する。
- 内部OOFはモデル選択にも使用するため、構成決定後に未使用の将来snapshotを最終holdoutとして評価する。

### 6.4 成果物と実行入口

- 実装: `hybrid_AI_model15/`
- 全工程: `hybrid_AI_model15/run_all.py`
- 意味JSON抽出: `hybrid_AI_model15/1_extract_semantic_json.py`
- 統合学習: `hybrid_AI_model15/2_train_model15.py`
- 結果表示: `hybrid_AI_model15/view_results.py`
- 学習済みモデル: `hybrid_AI_model15/artifacts/model15.joblib`
- OOF評価: `hybrid_AI_model15/artifacts/evaluation_model15.json`
- VS Codeの実行メニューにModel 15の全工程・各Step・結果表示を追加済み。

全工程はユーザーがVS Codeまたはコマンドから明示的に開始したときだけ実行する。

### 6.5 完了後監査と修正版（2026-08-09）

- 初回Model15はMAE 6,283円だったが、Qwen単体MAEが19,086円で、最終LightGBMのQwen gainは0.33%に留まった。
- 原因は `group_by_length` が評価・予測にも適用され、長さ順の予測を元のticket順として保存した行対応ずれだった。
- Qwen学習ログのfold log-MSE 0.17～0.48に対し、保存OOFは0.81～1.10であり、行ずれを実測で確認した。
- 評価・予測samplerをticket順へ固定し、fold log-MSEと全体MAEの自動品質ゲートを追加した。
- 既存5-fold `best_adapter`は正常なため、Qwenを再学習せずFP16で順序固定再推論する `repair_model15.py` を追加した。
- bf16回帰出力の粗さ、raw Huberの約3.5万円定数化、円MAEとearly stopping指標の不一致も修正した。
- Model13の `bante_x_baseprice`、`surikae_x_bante` を復活し、極端に疎い直接列はModel13同様に除外した。
- LLM JSONなし、Qwenなし、BERTなしを同一foldで測るアブレーションを保存する。
- `pipeline_version=model15_repaired_v2` と ordered Qwen schemaを持たない旧成果物は利用・表示を拒否する。
- 詳細: `hybrid_AI_model15/MODEL15_ACCURACY_REPAIR_REPORT.md`

### 6.6 特徴構成・学習方式の共同選択（2026-08-10）

- ordered Qwen修復後の主モデルはMAE 6,497円だったが、BERTなしは6,293円、Qwenなしは6,360円となり、固定的な全特徴統合が逆効果と判明した。
- 旧コードはアブレーションを評価しても本番候補に採用できず、最良候補と異なる特徴構成を `model15.joblib` に保存していた。
- `full`、`qwen_only`、`bert_only`、`tabular_only` の4特徴構成と、`log_l1`、`log_weighted`、`raw_huber` の3学習方式をOptunaの同一探索空間へ統合する。
- 12組合せすべてにbaseline trialを保証し、120 trialのTPE探索を有望な組合せへ集中させる。12組合せを各50 trialで独立探索する600 trial方式より効率的に、特徴選択と木のパラメータを同時最適化する。
- Model13式の平方根価格重み（指数0.5）はbaselineとして維持しつつ、価格重み指数0.20～0.80とraw Huberのalpha 0.70～0.95も条件付き探索する。
- 各組合せの最良trialを同一5-foldで再計算し、技術的健全性を満たす最小MAE候補と完全に同じ特徴構成・目的関数で本番モデルを学習する。
- LLM JSON意味特徴は全特徴構成に保持し、選択後の主構成・主目的・主パラメータを固定した公平な除外比較で寄与を測定する。
- `LGBMRegressor.predict`ではなく検証済みBoosterから予測し、疎行列の自動特徴名に関する誤解を招く警告を解消する。
- 実行入口は `hybrid_AI_model15/retrain_meta_model15.py`。既存のordered Qwen OOFとBERTキャッシュを読み取るだけで、Qwen学習・Qwen推論・BERT抽出は行わない。
- 最終成果物には `pipeline_version=model15_joint_feature_selection_v3`、選択されたfeature profile、model family、Qwen/BERTの実行要否を記録する。

---

## 7. Model16：全価格共通・nestedグローバルアンサンブル

Model15 v3はMAE 5,938円まで改善したが、全候補がLightGBM中心で残差相関0.90以上となり、
同系統モデルの追加探索だけでは改善が限定的だった。価格帯別モデルは実価格を使った恣意的な
選択に見える余地があるため完全に禁止し、全チケットへ同一の固定式を適用するModel16を構築する。

### 7.1 モデルと特徴の相性

- One-Hot LightGBM log-L1を主軸とし、Model15で有効だった表特徴・as-of市場特徴・LLM JSON意味特徴を維持する。
- `delivery_method`は1,335カテゴリあるため、決定規則による配送経路・時期の正規化を追加し、CatBoostではrawカテゴリもOrdered CTRとして扱う。
- 全行共通の逆価格学習重みを持つMAPE LightGBMを独立expertとし、価格帯分割なしで相対誤差への感度を追加する。
- BERT 768次元をtreeへ直結せず、fold-local PCA 16/32/48/64/96次元とRidgeで低容量expert化する。次元と正則化は内側foldだけで選択する。
- Qwenはordered OOF単体MAE 8,612円、Model15最適化後もMAEを261円悪化させたこと、既存OOFの内側fold流用がnested評価を壊すことから除外する。

### 7.2 不正を防ぐ評価方式

- 外側5-foldを完全な未使用評価、内側4-foldをOptuna・BERT次元・blend重み決定に使用する。
- duplicate descriptionは内外とも同一foldへ固定する。
- expert統合は全行共通の非負・総和1の固定重みのみ。価格、予測価格、価格帯による行別ルーティングは禁止する。
- 主指標は学習前に全件MAEへ固定し、固定blendのMAPEが最良MAE expertより0.10ポイント超悪化しない制約を置く。
- 各外側foldで全選択工程をやり直し、検証foldの実価格をハイパーパラメータ・次元・重みへ使用しない。

### 7.3 効率・再開

- LightGBMのfold-local補完・One-Hot行列をtrial間で再利用する。
- OptunaはSQLite studyで再開し、完了した外側foldの予測と設定をatomic checkpointで再利用する。
- trial並列化は行わず、各LightGBM/CatBoostがCPU全コアを使用してoversubscriptionを避ける。
- Qwen学習・Qwen推論・BERT再抽出・GPU処理は行わない。
- 実行入口は `hybrid_AI_model16/run_model16.py`。Codexは構築と学習なし検証だけを行う。

---

## 8. 意思決定支援モデル群（需要・代替出品・買い時）

Model16の成立価格推定を基礎としつつ、需要判断と買い時判断は目的変数が異なるため、
`decision_support_models/` に独立した3モデルとして構築する。共有するのはrawデータと
CSV形式のOOF／推論結果だけで、モデルフォルダ間のPython importは行わない。

### 8.1 需要状態

- 出品中の各日をランドマークとし、1・3・7日以内の `active / sold / deleted` を予測する。
- deletedは他媒体売却・取消を区別できないためsoldへ統合しない。
- 重複説明文をfold間で分離し、学習ラベル期限が検証開始を越える行をpurgeする。
- 評価は確率のlog loss、Brier、ECE、クラス別ROC-AUC・PR-AUCとする。
- 最新snapshotの全説明文を価格非依存Qwen JSONへ変換する。既存Model15キャッシュは
  Qwen15 schemaと説明文hashが一致するものだけ再利用し、legacy・parse errorは再抽出する。
- 意味JSONが全ticketを覆わなければ学習を拒否する。各期間の同一foldで
  `tabular / semantic` を比較し、log loss改善かつBrier非悪化の場合だけ採用する。

### 8.2 安価な代替出品

- 同一イベント・枚数・券種・名義種別で、現在より `max(2,000円, 5%)` 以上安い
  新規出品が1・3・7日以内に現れる確率を予測する。
- 値下げ履歴がない現状でも、`first_observed_at` による新規流入は学習可能とする。
- 将来最安値・出現時刻は評価用に保存するが、特徴量から強制除外する。
- 同じ全ticket用意味JSONを候補にし、log loss・Brier・PR-AUCを守る場合だけ期間別採用する。

### 8.3 買い時

- 前2モデルのOOFと妥当価格をCSVで受け取り、割安率・消失確率・代替出現確率を統合する。
- 価格帯別ルーティングはせず、1・3・7日の期間ごとに方針係数を選ぶ。
- 時系列前半70%で方針を選択し、後半30%の平均機会損失を最終評価する。
- 利用者の損失設定だけは `safety / balanced / savings` の3通りを明示し、モデル精度と
  価値判断を混同しない。

実装・実行順・最終holdout計画は `decision_support_models/README.md` と
`decision_support_models/EVALUATION_PLAN.md` に記載する。Codexは構築、preflight、
合成データ試験のみ行い、実学習はユーザーが明示的に開始した場合だけ実行する。

Qwen価格回帰とBERT埋め込みは既存Model15/16のアブレーションで悪化したため、意思決定
モデルには復活させない。LLMは価格を生成せず意味抽出だけに限定し、買い時方針へは
需要・代替モデルの未使用時系列予測を介して間接的に反映する。
