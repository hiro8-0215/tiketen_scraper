# Semantic feature snapshot (2026-09-23)

This directory preserves the local semantic features and their generation manifest as of 2026-09-23. The CSV contains description hashes and derived categorical features; it does not contain the source ticket descriptions. The source snapshot was `tiketen_date_data/data_9_18`.

The active model pipeline still reads and updates its local `semantic_feature_data/` directory. This dated copy is for versioned reference and does not change scraper inputs or outputs.
