# チケット相場予測AI（Hybrid Model 8）評価・検証スキーム

本ドキュメントでは、スクレイピングで収集したデータを用いて構築されたハイブリッドAIモデル（Qwen 7B + BERT + LightGBM）の「評価方法」「最適化アプローチ」および「今後の検証（Validation）戦略」についてまとめます。

## 1. 全体的な評価フレームワーク

現在のシステム（`3_train_lgbm.py` 等）では、LLM（自然言語）の推論結果・BERTのベクトル情報・表形式データ（座席や日付など）を統合して最終予測を行うLightGBMを採用しており、以下の評価フレームワークで構築されています。

### 1-1. ハイパーパラメータの最適化 (Optuna)
* **手法**: Optunaを用いた自動パラメータ探索（50回のトライアル）
* **最適化指標**: **RMSE**（Root Mean Squared Error / 二乗平均平方根誤差）
  * ※価格の予測において、極端に相場から外れた「大ハズレ」を強くペナルティ化するためにRMSEをターゲットにしています。
* **CV（交差検証）**: 4-Fold CV を使用し、未知データに対する汎化性能を検証しながらパラメータを決定しています。

### 1-2. 最終モデルの精度検証 (Out-Of-Fold; OOF)
最適化されたパラメータを用いて、最終的に **5-Fold Cross Validation (5分割交差検証)** で評価を行います。

* 学習データを5つに分割し、「4つで学習、1つでテスト」を5回繰り返すことで、**全データに対する未知データとしての予測値（OOF予測値）** を算出します。
* これにより、過学習（オーバーフィッティング）を起こしていないリアルな予測精度を検証しています。

---

## 2. 評価指標 (Metrics)

対数化（`log1p`）して学習した予測値を元の「円（¥）」単位に戻したうえで、以下の3つの指標で評価しています。

| 指標 | 意味 | 検証における重要性 |
| :--- | :--- | :--- |
| **MAE** <br>(Mean Absolute Error) | **平均絶対誤差（円）**<br>予測価格と実際の価格のズレの平均値。 | 「平均して何円くらいの誤差で予測できているか」という、人間にとって直感的に最もわかりやすい精度の指標です。 |
| **RMSE** <br>(Root Mean Squared Error) | **二乗平均平方根誤差（円）**<br>誤差を二乗して平均し、ルートをとった値。 | 外れ値（極端に高い/安い予測ミス）がある場合に数値が跳ね上がるため、モデルの「安定性」を検証するのに役立ちます。 |
| **R²** <br>(R-squared / 決定係数) | **予測の当てはまりの良さ**<br>1.0に近いほど完璧な予測。 | チケット価格のバラツキをどれだけモデルが説明できているかを示します。一般的に相場予測で0.8以上あれば高精度と見なせます。 |

---

## 3. 情報リーク（未来情報）への対策と検証

機械学習において最も危険な「データリーク（予測時点では知り得ない未来のデータを使ってしまうこと）」を防ぐため、データローダーで以下のような検証・対策を行っています。

> [!IMPORTANT]
> **除外されている特徴量:**
> * `listing_duration_days`（出品されていた日数）: チケットが売れるまでにかかった日数は「売れた後」にしか確定しない未来の情報であるため、意図的に除外して学習させています。
> * その他、ID類や純粋なテキストなど、直接的なリークに繋がる変数をフィルターしています。

---

## 4. 今後どのように検証・分析を進めていくか

ただ精度を出すだけでなく、**「なぜその価格になるのか」** を検証していくことでシステム全体の価値が高まります。今後は以下の3軸で検証を進めるのが有効です。

### ① 特徴量重要度（Feature Importance）の検証
スクリプト内で出力される `特徴量重要度 TOP 30`（Gainベース）を確認し、「モデルがどの要素を重視して価格を弾き出したか」を人間がレビューします。
* 例：「FC会員数とキャパシティから出した倍率」が重要度上位に来ていれば、モデルは真っ当な判断をしている。
* 例：「特定の出品者名」などが上位に来ている場合、転売ヤーの特定要因になっているか、あるいは過学習している可能性があるため分析する。

### ② 散布図による「大ハズレ（外れ値）」の分析
出力される「実際の価格 vs 予測価格」の散布図において、**「理想的な予測線から大きく外れているプロット（点）」** を抽出して中身を確認します。
* 「AIは安いと判断したのに、実際は超高値で売れたチケット」の備考欄を読むことで、**「今のAI（LLMや特徴量）が拾いきれていないプレミア条件」**（例：ファンサ確実の特定ブロックなど）を新たに発見でき、それを特徴量抽出ロジック（`description_parser.py`）に還元できます。

### ③ 時系列ベースの検証 (Time-Series Validation)
現在のK-Foldは「ランダム分割」ですが、現実の運用に近づけるための究極の検証として **「過去のデータで学習し、未来（最新1週間など）のデータでテストする」** 検証方法も今後導入する価値があります。これにより、「アイドルのトレンド変化」にAIが追従できているかを確認できます。
